Thank you for your order.

Your <%= @app.name %> order #<%= @card_order.order_number %> is being processed. This is your receipt.

We are printing your card<%= @card_printings.size > 1 ? "s" : "" %> for <%= @card_printings.map { |p| p.name_or_self("yourself") }.to_sentence %> and will notify you once <%= @card_printings.size > 1 ? "they" : "it" %> ha<%= @card_printings.size > 1 ? "ve" : "s" %> been mailed.

Your account has been charged for <%= @card_printings.size %> card<%= @card_printings.size > 1 ? "s" : "" %>.

Total charges: <%= @card_order.printable_total_price %>

<% if @card_order.allocated_credits > 0 %>
Total credits: <%= @card_order.allocated_credits %>
<% end %>
